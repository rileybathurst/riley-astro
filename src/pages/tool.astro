---
import Trailhead from "../templates/trailhead.astro";
import fetchApi from "../lib/strapi";

const tools = await fetchApi({
  endpoint: "tools",
});
---

<Trailhead trail="Tools" breadcrumb="tools" paths={tools} />
