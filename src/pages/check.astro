---
import Trailhead from "../templates/trailhead.astro";
import fetchApi from "../lib/strapi";

type checkTypes = {
  name: string;
  slug: string;
};
const checks: checkTypes[] = await fetchApi({
  endpoint: "checks",
});
---

<Trailhead trail="Checks" paths={checks} />
