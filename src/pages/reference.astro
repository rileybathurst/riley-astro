---
import Trailhead from "../templates/trailhead.astro";
import fetchApi from "../lib/strapi";

const references = await fetchApi({
  endpoint: "references",
});
---

<Trailhead trail="Reference" breadcrumb="reference" paths={references} />
