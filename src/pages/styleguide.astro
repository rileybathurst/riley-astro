---
import Trailhead from "../templates/trailhead.astro";
import fetchApi from "../lib/strapi";

const styleguides = await fetchApi({
  endpoint: "styleguides",
});
---

<Trailhead trail="Styleguide" breadcrumb="styleguide" paths={styleguides} />
