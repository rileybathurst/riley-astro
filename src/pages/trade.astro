---
import Trailhead from "../templates/trailhead.astro";
import fetchApi from "../lib/strapi";

type tradeTypes = {
  title: string;
  slug: string;
};
const trades: tradeTypes[] = await fetchApi({
  endpoint: "trades",
});
---

<Trailhead trail="trade" paths={trades} breadcrumb="trade" />
