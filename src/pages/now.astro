---
import Layout from "../components/layout.astro";
import { formatDate } from "../components/format-date";

import {
  BlocksRenderer,
  type BlocksContent,
} from "@strapi/blocks-react-renderer";

type NowType = {
  id: number;
  description: BlocksContent;
  createdAt: string;
  updatedAt: string;
};

import fetchApi from "../lib/strapi";

const nows: NowType[] = await fetchApi({
  endpoint: "nows", // the content type to fetch
  more: false,
});

console.log(nows);
---

<Layout title="Now">
  <main class="gutters">
    <h1 class="fore">Now</h1>

    <section class="cover split">
      {
        nows.map((now) => (
          <article>
            <p>Created at: {formatDate(now.createdAt)}</p>

            {now.updatedAt !== now.createdAt && (
              <p>Updated at: {formatDate(now.updatedAt)}</p>
            )}
            <BlocksRenderer content={now.description} />
            <hr />
          </article>
          <div >
            {/* // TODO: */}
            now.image
          </div>
        ))
      }
    </section>
  </main>
</Layout>
