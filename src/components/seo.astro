---
import fetchApi from "../lib/strapi";
import type { AboutType } from "../types/about-type";

const meta: AboutType[] = await fetchApi({
  endpoint: "meta", // the content type to fetch
  //
  more: false,
});
---

<title>{meta[0].name}</title>
``
